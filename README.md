# AI bot for Matrix
This is a simple Matrix bot that uses Gemini to generate responses to user messages.

# How to use?
Just invite this bot `@megatron:unifwe.com` to your room. Then mention it. (Reply like twitter are supported)

# Self-Hosting
Build from source using cargo: 
```
cargo build --release
```

First look at `.env.example` and fill it with your own credentials.
```
cargo run --release
```

### Or simply download a binary from release

By `knight:unifwe.com`
